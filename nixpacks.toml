# Nixpacks configuration (for Nixpacks deployments)
[phases.setup]
nixPkgs = ["nodejs", "postgresql", "git"]

[start]
cmd = "bash -c 'node scripts/migrate.cjs && node .next/standalone/server.js & npx tsx src/server/worker.ts & wait -n'"
